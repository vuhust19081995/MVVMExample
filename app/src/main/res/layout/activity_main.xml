<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <data>
        <variable
            name="viewModel"
            type="com.workspace.mvvmlogin.screen.main.MainViewModel"
            />
        <variable name="view" type="android.view.View"/>
    </data>
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.workspace.mvvmlogin.screen.main.MainActivity"
        >

        <android.support.v7.widget.RecyclerView
            android:id="@+id/recycler_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:recyclerAdapter="@{viewModel.userAdapter}"
            app:layoutManager="LinearLayoutManager"
            >
        </android.support.v7.widget.RecyclerView>

        <ProgressBar
            android:visibility="@{viewModel.complete ? view.GONE : view.VISIBLE}"
            android:layout_centerInParent="true"
            android:layout_width="50dp"
            android:layout_height="50dp"
            />

    </RelativeLayout>
</layout>
